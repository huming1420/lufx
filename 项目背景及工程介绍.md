# 陆控经营分析看板 LLM 改造项目需求背景与工程结构说明

## 1. 项目背景

当前项目是一个面向陆控贷款业务的经营分析看板，已有两个核心前端页面：

```text
lufax_dashboard3.html
ai_insights.html
```

其中：

* `lufax_dashboard3.html` 是主经营看板，包含净利润、贷款规模、余额结构、新增放款、ROA 拆解、定价、风险成本、资金成本、业务成本等模块。
* `ai_insights.html` 是 AI 经营洞察页，包含动态解读和经营计算器两个部分。

目前页面已经具备较完整的 UI、图表和经营分析框架，但核心问题是：

```text
页面中的“AI洞察”和“文字洞察”当前都是写死的静态文案，并没有真正根据数据动态生成。
```

本次项目目标是将静态 HTML 看板改造为：

```text
Excel 数据上传
→ 数据库存储
→ 指标计算与规则判断
→ 结构化 Fact Pack
→ 调用内部 LLM / 智能体平台
→ 返回结构化 JSON 洞察
→ 前端动态渲染
```

---

# 2. 项目目标

## 2.1 核心目标

将当前写死的经营分析文案改造成由 AI 动态生成，同时保证：

```text
1. 指标计算准确
2. 经营逻辑可控
3. LLM 不胡编数字
4. 输出结构稳定
5. 后续可接入内部 LLM 或智能体平台
6. 前端页面样式和原有交互尽量不破坏
```

## 2.2 改造后的能力

改造完成后，系统应支持：

```text
1. 通过 Excel 上传经营数据
2. 将 Excel 数据解析并入库
3. 自动生成看板核心指标
4. 自动计算同比、环比、预算差、红黄绿灯、杜邦拆解、根因贡献
5. 为每个看板卡片生成 AI 文字洞察
6. 为 AI 洞察页生成动态经营诊断
7. 为经营计算器生成情景解释
8. 支持内部 LLM / 智能体平台替换
9. 支持 Mock LLM 便于本地开发
```

---

# 3. 当前页面现状

## 3.1 `lufax_dashboard3.html`

当前主看板中，每个经营卡片都有类似结构：

```html
<div class="insight-text" id="insight-risk">
  写死的经营洞察文案
</div>
```

当前问题：

```text
1. 洞察文案写死在 HTML 中
2. 编辑按钮只支持前端手工编辑
3. 不读取真实指标数据
4. 不调用 LLM
5. 不根据预算差、红黄灯、风险指标自动变化
```

需要改造为：

```text
前端根据 data-card-id 调用后端接口
→ 后端生成 CARD_FACT_PACK
→ LLM 返回结构化 JSON
→ 前端渲染 standard_insight、drivers、watch_items、exploratory_insights
```

---

## 3.2 `ai_insights.html`

当前 AI 洞察页中，动态解读部分依赖静态数组：

```javascript
AI_DIMS = [...]
```

当前问题：

```text
1. 六维诊断分数写死
2. 综合评分只是静态数据求平均
3. 根因链写死
4. 抵消因子写死
5. 不根据真实指标变化
```

需要改造为：

```text
前端调用 /api/insights/dashboard
→ 后端生成 DASHBOARD_FACT_PACK
→ LLM 返回经营诊断 JSON
→ 前端渲染综合评分、六维诊断、根因链、探索发现
```

---

## 3.3 经营计算器

当前经营计算器已有确定性前端公式：

```text
ROA = 定价率 - 信贷损失率 - 资金成本 - 销售成本 - 运营成本 - 税及其他

净利润 = ANR × ROA / 100
```

当前问题：

```text
1. 参数是写死的
2. 情景解释是规则文案
3. 没有接入 LLM 做深度解释
```

需要改造为：

```text
前端保留确定性公式计算
→ 用户点击“生成 AI 情景解读”
→ 后端生成 SCENARIO_FACT_PACK
→ LLM 只解释已经计算好的结果
```

注意：

```text
经营计算器中，LLM 不负责计算 ROA 和净利润，只负责解释。
```

---

# 4. 核心设计原则

## 4.1 计算和解释分离

必须坚持：

```text
后端 / 前端确定性代码负责计算
LLM 只负责解释
```

LLM 不允许负责：

```text
1. 计算 ROA
2. 计算净利润
3. 计算同比 / 环比
4. 计算预算差
5. 计算红黄绿灯
6. 计算六维评分
7. 计算利润贡献
8. 计算敏感性矩阵
```

LLM 可以负责：

```text
1. 经营语言表达
2. 跨指标矛盾识别
3. 隐藏风险提示
4. 结构性变化判断
5. 管理层追问方向
6. 情景解释
```

---

## 4.2 Fact Pack 是 AI 唯一输入

不要让 LLM 直接读取 Excel、HTML 或数据库原始表。

正确方式是：

```text
数据库指标
→ 后端计算和规则判断
→ 生成结构化 Fact Pack
→ LLM 基于 Fact Pack 生成洞察
```

三个 AI 场景分别对应：

```text
CARD_FACT_PACK       单卡片洞察
DASHBOARD_FACT_PACK  AI 动态解读
SCENARIO_FACT_PACK   经营计算器情景解读
```

---

## 4.3 标准洞察 + 探索洞察

为了避免输出过于机械，同时控制幻觉，采用双层输出：

```text
standard_insight / standard_diagnosis / standard_explanation
= 稳定、可审计、可直接展示

exploratory_insights / emerging_findings / scenario_exploration
= 允许模型做深度挖掘，但必须绑定证据、置信度和验证项
```

---

# 5. 三个 AI 场景说明

## 5.1 场景一：单卡片文字洞察

对应页面：

```text
lufax_dashboard3.html
```

对应接口：

```http
POST /api/insights/card
```

请求示例：

```json
{
  "period": "2026-03-YTD",
  "card_id": "risk",
  "force_refresh": false
}
```

输出核心字段：

```json
{
  "card_id": "risk",
  "status": "red",
  "title": "风险成本压制ROA",
  "standard_insight": "3月YTD信贷损失率为7.8%，距离8.0%管理红线仅0.2pct，并高于预算1.1pct；风险压力集中在易贷和2024Q1 Vintage，短期仍将压制ROA修复。",
  "drivers": [],
  "watch_items": [],
  "evidence": [],
  "exploratory_insights": [],
  "deep_dive_questions": []
}
```

主要用途：

```text
替换每个卡片里原来写死的 insight-text 文案。
```

---

## 5.2 场景二：AI 动态解读

对应页面：

```text
ai_insights.html
```

对应接口：

```http
POST /api/insights/dashboard
```

请求示例：

```json
{
  "period": "2026-03-YTD"
}
```

输出核心字段：

```json
{
  "period": "2026-03-YTD",
  "health_score": 60,
  "health_label": "中性观察",
  "standard_diagnosis": {
    "health_summary": "当前经营处于中性观察状态，信贷损失率上行是主要拖累，资金成本下降和运营成本改善形成部分对冲，后续关键在于风险暴露能否回落。",
    "verdict": {
      "level": "warn",
      "text": "需重点关注风险暴露节奏"
    },
    "summary_items": []
  },
  "dimensions": [],
  "root_cause_chain": [],
  "offset_factors": [],
  "emerging_findings": [],
  "management_questions": [],
  "watch_items": []
}
```

主要用途：

```text
替换 ai_insights.html 中写死的 AI_DIMS、综合评分、根因链和动态摘要。
```

---

## 5.3 场景三：经营计算器情景解读

对应页面：

```text
ai_insights.html 的经营计算器 Tab
```

对应接口：

```http
POST /api/insights/scenario
```

请求由前端当前滑块参数和计算结果组成。

输出核心字段：

```json
{
  "scenario": "base",
  "scenario_label": "基准情景",
  "standard_explanation": {
    "judgement": "当前参数情景下预测ROA为4.25%，预测净利润为80.8亿，利润结果对信贷损失率假设高度敏感。",
    "profit_driver": "利润主要来自定价率18.2%与总成本率13.95%之间的利差空间。",
    "key_variable": "信贷损失率",
    "risk_boundary": "信贷损失率继续上行将直接压缩ROA和净利润空间。",
    "upside_condition": "风险成本稳定且ANR不下滑时利润修复更稳。",
    "sensitivity_comment": "信贷损失率每变动0.5pct影响净利润9.5亿。",
    "management_commentary": "当前情景不是单纯规模驱动，而是由定价、风险成本和ANR共同决定。"
  },
  "scenario_exploration": {
    "fragile_assumption": {},
    "hidden_risk": {},
    "upside_surprise": {}
  },
  "watch_items": []
}
```

主要用途：

```text
为经营计算器提供情景解释、脆弱假设、隐藏风险和上行条件。
```

---

# 6. 推荐工程结构

## 6.1 总体目录

```text
project/
├─ lufax_dashboard3.html
├─ ai_insights.html
├─ backend/
│  ├─ server.py
│  ├─ services/
│  │  ├─ upload_service.py
│  │  ├─ metric_normalizer.py
│  │  ├─ metric_calc_service.py
│  │  ├─ rule_engine.py
│  │  ├─ fact_builder.py
│  │  ├─ insight_service.py
│  │  ├─ llm_adapter.py
│  │  ├─ mock_llm.py
│  │  └─ paic_agent_adapter.py
│  ├─ prompts/
│  │  ├─ card_insight_v2.txt
│  │  ├─ dashboard_insight_v2.txt
│  │  └─ scenario_insight_v2.txt
│  ├─ schemas/
│  │  ├─ card_insight.schema.json
│  │  ├─ dashboard_insight.schema.json
│  │  └─ scenario_insight.schema.json
│  ├─ data/
│  │  ├─ dashboard_metrics_mock.json
│  │  └─ card_config.json
│  └─ db/
│     └─ app.db
```

---

## 6.2 目录职责

### `backend/server.py`

FastAPI 入口文件。

负责：

```text
1. 注册 API 路由
2. 托管前端 HTML
3. 提供健康检查
4. 调用 insight_service
```

核心接口：

```http
GET  /api/health
POST /api/upload/excel
GET  /api/dashboard/data
POST /api/insights/card
POST /api/insights/dashboard
POST /api/insights/scenario
```

---

### `backend/services/upload_service.py`

负责 Excel 上传。

职责：

```text
1. 接收 Excel 文件
2. 保存文件
3. 生成 batch_id
4. 读取 Sheet
5. 写入 raw_metric_data
```

---

### `backend/services/metric_normalizer.py`

负责指标标准化。

职责：

```text
1. 将 Excel 原始指标映射为 metric_code
2. 统一单位
3. 统一期间
4. 写入 dashboard_metric
```

---

### `backend/services/metric_calc_service.py`

负责确定性计算。

职责：

```text
1. 计算同比
2. 计算环比
3. 计算预算差
4. 计算 ROA
5. 计算净利润
6. 计算利润 bridge
7. 计算敏感性
```

注意：

```text
这些计算不能交给 LLM。
```

---

### `backend/services/rule_engine.py`

负责规则判断。

职责：

```text
1. 红黄绿灯判断
2. 异常标签生成
3. 六维评分
4. 根因排序
5. 抵消因子识别
6. cross_signals 识别
```

输出结果会进入 Fact Pack。

---

### `backend/services/fact_builder.py`

负责构造三类 Fact Pack。

核心方法：

```python
build_card_fact_pack(period, card_id)
build_dashboard_fact_pack(period)
build_scenario_fact_pack(payload)
```

这是 AI 输入的核心。

---

### `backend/services/insight_service.py`

负责 AI 洞察主流程。

职责：

```text
1. 根据场景选择 Prompt
2. 生成 Fact Pack
3. 调用 LLM Adapter
4. 校验 JSON Schema
5. 校验数字白名单
6. 保存 insight_result
7. 返回前端
```

---

### `backend/services/llm_adapter.py`

统一 LLM 适配层。

要求：

```text
1. 默认使用 MockLLMAdapter
2. 可切换内部 LLM
3. 可切换智能体平台
4. 不在前端暴露 AppKey
5. 不在代码中写死 Token
```

建议支持：

```text
LLM_PROVIDER=mock
LLM_PROVIDER=paic_agent
LLM_PROVIDER=internal_llm
```

---

### `backend/services/mock_llm.py`

本地开发用。

作用：

```text
1. 没有真实 LLM 时返回模拟 JSON
2. 保证前端流程能跑通
3. 用于本地测试和 UI 联调
```

---

### `backend/services/paic_agent_adapter.py`

内部智能体平台适配器。

根据内部文档流程：

```text
1. 获取 Token
2. 创建会话
3. 发送消息
4. 获取响应
5. 提取 JSON
```

配置通过环境变量读取：

```env
PAIC_AGENT_GATEWAY_URL=
PAIC_AGENT_APP_ID=
PAIC_AGENT_APP_KEY=
PAIC_AGENT_BOT_ID=
```

---

### `backend/prompts/`

存放三类 Prompt。

```text
card_insight_v2.txt
dashboard_insight_v2.txt
scenario_insight_v2.txt
```

作用：

```text
告诉 LLM 如何基于 Fact Pack 生成结构化洞察。
```

---

### `backend/schemas/`

存放 JSON Schema。

```text
card_insight.schema.json
dashboard_insight.schema.json
scenario_insight.schema.json
```

作用：

```text
校验 LLM 输出格式，防止模型乱输出。
```

---

### `backend/data/`

开发阶段模拟数据和配置。

```text
dashboard_metrics_mock.json
card_config.json
```

说明：

```text
dashboard_metrics_mock.json = 模拟数据库指标
card_config.json = 卡片配置
```

后续接数据库后：

```text
dashboard_metrics_mock.json 可被数据库替代
card_config.json 可继续保留，也可迁移到 card_config 表
```

---

# 7. 数据库设计建议

## 7.1 `excel_upload_batch`

记录 Excel 上传批次。

```text
id
filename
period
upload_user
upload_time
status
error_message
file_path
version
```

---

## 7.2 `raw_metric_data`

保存 Excel 原始解析结果。

```text
id
batch_id
sheet_name
row_no
col_name
raw_metric_name
raw_dimension
raw_segment
raw_period
raw_value
raw_unit
parsed_value
created_at
```

---

## 7.3 `dashboard_metric`

标准指标表，是 AI 和图表的核心数据源。

```text
id
period
metric_code
metric_name
value
display_value
unit
yoy
display_yoy
mom
display_mom
budget
budget_gap
display_budget_gap
redline
yellowline
distance_to_redline
display_distance_to_redline
status
direction
module
dimension
segment
product_type
channel_type
vintage
mob
customer_type
source_sheet
batch_id
created_at
```

---

## 7.4 `card_config`

卡片配置表。

```text
card_id
section_id
insight_element_id
title
level
view_type
prompt_template
schema_name
parent_metric
management_question
dupont_path_json
analysis_focus_json
metric_codes_json
segment_dimensions_json
allowed_exploration_types_json
enabled
display_order
```

---

## 7.5 `scenario_default_param`

经营计算器默认参数表。

```text
id
period
scenario
scenario_label
anr
consumer_finance_growth
pricing_rate
credit_loss_rate
npl_rate
funding_cost_rate
sales_cost_rate
opex_rate
tax_other_rate
non_loan_profit
created_at
```

---

## 7.6 `insight_result`

AI 洞察结果表。

```text
id
period
insight_type
card_id
prompt_version
schema_name
status
title
standard_insight
standard_diagnosis_json
standard_explanation_json
drivers_json
watch_items_json
evidence_json
exploratory_insights_json
emerging_findings_json
scenario_exploration_json
deep_dive_questions_json
management_questions_json
raw_fact_pack_json
raw_llm_output_json
validated
validation_error
created_at
```

---

# 8. 前端改造要求

## 8.1 `lufax_dashboard3.html`

### 改造前

```html
<div class="insight-text" id="insight-risk">
  写死洞察
</div>
```

### 改造后

```html
<div class="insight-text" id="insight-risk" data-card-id="risk">
  正在生成洞察...
</div>
```

新增函数：

```javascript
async function loadCardInsights(period) {
  const nodes = document.querySelectorAll('.insight-text[data-card-id]');

  for (const node of nodes) {
    const cardId = node.dataset.cardId;

    const res = await fetch('/api/insights/card', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        period,
        card_id: cardId,
        force_refresh: false
      })
    });

    const data = await res.json();
    node.textContent = data.standard_insight || data.insight || '暂无洞察';

    renderInsightTags(cardId, data);
    renderExploratoryInsights(cardId, data);
  }
}
```

---

## 8.2 `ai_insights.html`

### 改造前

```javascript
var AI_DIMS = [...]
```

### 改造后

```javascript
let AI_DIMS = [];

async function loadAiInsights() {
  const res = await fetch('/api/insights/dashboard', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ period: CURRENT_PERIOD })
  });

  const data = await res.json();

  AI_DIMS = data.dimensions || [];

  renderAiHeader(data);
  buildAiDims();
  renderRootCauseChain(data.root_cause_chain || []);
  renderOffsetFactors(data.offset_factors || []);
  renderEmergingFindings(data.emerging_findings || []);
  renderManagementQuestions(data.management_questions || []);
}
```

---

## 8.3 经营计算器

保留现有前端计算：

```text
updatePredict()
renderAiSankey()
renderSensTable()
```

新增按钮：

```html
<button type="button" onclick="generateScenarioInsight()">
  生成 AI 情景解读
</button>
```

新增函数：

```javascript
async function generateScenarioInsight() {
  const payload = collectScenarioPayload();

  const res = await fetch('/api/insights/scenario', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(payload)
  });

  const data = await res.json();
  renderScenarioInsight(data);
}
```

---

# 9. LLM 接入方式

## 9.1 推荐架构

```text
前端页面
→ 后端 API
→ Fact Pack
→ LLM Adapter
→ Mock / 内部 LLM / 智能体平台
```

不要采用：

```text
前端 HTML 直接调用内部 LLM
```

原因：

```text
1. AppKey 泄露风险
2. 无法统一校验
3. 无法缓存
4. 无法记录 Fact Pack
5. 无法做失败兜底
```

---

## 9.2 环境变量

```env
LLM_PROVIDER=mock

PAIC_AGENT_GATEWAY_URL=
PAIC_AGENT_APP_ID=
PAIC_AGENT_APP_KEY=
PAIC_AGENT_BOT_ID=

INTERNAL_LLM_BASE_URL=
INTERNAL_LLM_API_KEY=
INTERNAL_LLM_MODEL=
```

---

# 10. 校验与兜底

## 10.1 必须做 JSON Schema 校验

流程：

```text
LLM 输出
→ 提取 JSON
→ json.loads()
→ JSON Schema 校验
→ 数字白名单校验
→ 通过后返回前端
```

## 10.2 数字白名单校验

要求：

```text
LLM 输出中的所有数字必须能在 Fact Pack 中找到。
```

不允许：

```text
模型自己编造行业均值
模型自己编造预测值
模型自己计算预算差
模型自己创造红线
```

## 10.3 失败兜底

如果 LLM 输出失败：

```text
1. 重试一次
2. 仍失败则返回规则模板文案
3. 如果规则模板也失败，返回“暂无洞察”
```

---

# 11. 开发优先级

## 第一阶段：工程骨架跑通

目标：

```text
1. FastAPI 后端可启动
2. 前端能通过接口读取 Mock 洞察
3. 三个 AI 接口可返回结构化 JSON
4. Schema 校验可用
5. 不依赖真实 LLM
```

必须完成：

```text
/api/health
/api/insights/card
/api/insights/dashboard
/api/insights/scenario
```

---

## 第二阶段：前端去静态化

目标：

```text
1. lufax_dashboard3.html 的 insight-text 不再写死
2. ai_insights.html 的 AI_DIMS 不再写死
3. 经营计算器新增 AI 情景解读按钮
```

---

## 第三阶段：接入 Excel 上传和数据库

目标：

```text
1. 支持 Excel 上传
2. 支持指标解析
3. 支持 dashboard_metric 入库
4. 支持从数据库生成 Fact Pack
```

---

## 第四阶段：接入内部 LLM / 智能体平台

目标：

```text
1. MockLLMAdapter 替换为 PAICAgentAdapter 或 InternalLLMAdapter
2. 前端不用改
3. 后端接口不用改
4. 只替换 LLM Provider
```

---

# 12. AI 编程工具注意事项

在让 AI 修改代码时，请遵守：

```text
1. 不要破坏原有页面样式
2. 不要删除 ECharts 图表逻辑
3. 不要让 LLM 参与计算
4. 所有 fetch 使用相对路径
5. 不要在 HTML 中写死 LLM 地址
6. 不要在前端暴露 AppKey
7. Prompt 文件化
8. Schema 文件化
9. Mock LLM 可跑通
10. 内部 LLM 通过 Adapter 替换
```

---

# 13. 关键判断

本项目的核心不是“让 LLM 写几段话”，而是建设一个可控的经营分析解释层。

正确架构是：

```text
数据层：Excel / 数据库
计算层：指标计算、预算差、杜邦拆解
规则层：红黄绿灯、六维评分、根因排序
事实层：Fact Pack
LLM 层：标准洞察 + 探索洞察
校验层：Schema + 数字白名单
展示层：经营看板与 AI 洞察页
```

最终效果应该是：

```text
主结论稳定可靠；
探索发现有一定惊喜；
所有推测都有证据和验证项；
所有数字可追溯；
前端、后端、LLM 解耦。
```
