
## 15:37
- 实现了 ExternalLlmAdapter.doCall() 方法，对接平安 PAIC Agent 网关
- 支持 paic / openai 双协议，默认 paic
- 配置：gateway=http://agents-api-sze.paic.com.cn, appId=ZHJYPT, botId=1089944, model=Qwen3.6-35B
- 添加了 httpclient 4.5.14 依赖
- 更新了 LlmConfig.java 注入 paicAppId / paicBotId
- 更新了 application.properties 和工程文档

## 15:00 (续)
- **Task #8 完成**：修复全部 7 个测试文件的 18 处编译错误：
  1. `RuleEngineServiceTest.java`：4 处 `result.gettotal_score()` → `(Double) result.get("total_score")`（Map 不存在此方法）
  2. `FactBuilderServiceTest.java`：补加缺失的 `import com.lufax.dashboard.TestDataFactory`
  3. `InsightServiceTest.java`：补加缺失的 `import com.lufax.dashboard.TestDataFactory`
  4. `InsightJobServiceTest.java`：补加缺失的 `import com.lufax.dashboard.TestDataFactory`
  5. `InsightResultMapperTest.java`：4 处 `.setError()`/`.getError()` → `.setErrorMessage()`/`.getErrorMessage()`
  6. `InsightJobMapperTest.java`：2 处 `.getError()` → `.getErrorMessage()`
  7. `InsightJobItemMapperTest.java`：3 处 `.getError()` → `.getErrorMessage()`
- 根本原因：包重构后 entity 字段名为 errorMessage（非 error），RuleEngineService.evaluate() 返回 Map 需用 .get() 访问
- DashboardControllerTest.java 和 JsonUtilsTest.java 确认无需修改

## 16:52
- **全部 13 个任务已完成**，Task #8/#9/#11/#12/#13 全部标记为 completed
- 验证确认：所有 18 处修复均已生效（grep 扫描无遗漏）
- 剩余 12 个测试文件（5 controller + 2 llm + 5 util）import 路径均正确
- 项目当前无 Maven 环境（无 mvn、无 mvnw），无法运行 test-compile 验证
- 测试代码编写任务整体完成：19 个测试文件覆盖全栈（util→service→controller→repository→llm）

## 19:21
- **关键 Bug 修复**：ExternalLlmAdapter.java 中 fact_pack 数据未传入 LLM
  - 根因：doPaicCall/doOpenAiCall 仅发送 prompt 文本，params（factPayload）中的核心数据（core_metric/dimensions/cross_signals）被丢弃
  - 修复：新增 buildUserContent() 方法，将 params 中的非元数据键（排除 system/temperature/max_tokens）序列化为 JSON，以 "FACT_PACK:\n{...}" 格式附加到 prompt 末尾
  - 影响范围：PAIC 和 OpenAI 两条协议路径均已修复
  - Python 版 llm_adapter.py 无此问题（它通过 json.dumps 将 fact_pack 嵌入 user message）
